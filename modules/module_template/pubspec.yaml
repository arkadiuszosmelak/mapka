name: module_template
description: Template feature module. Copied by tools/create_module.sh — not a
  real package (excluded from the Melos workspace).
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  core:
    path: ../core
  design_system:
    path: ../design_system
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  go_router: ^17.3.0
  hooked_bloc: ^1.7.0
  injectable: ^3.0.0

dev_dependencies:
  build_runner: ^2.15.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^3.0.2
