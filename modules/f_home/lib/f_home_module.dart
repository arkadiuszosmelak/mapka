import 'package:core/core.dart';

/// What the home module contributes to the app shell. Pure UI for now — no DI,
/// guards or observers. Add them here when the feature grows.
class HomeModule extends ModuleManifest {
  const HomeModule();
}
