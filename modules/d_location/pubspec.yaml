name: d_location
description: Location data module — GPS permission, current position and a movement stream.
publish_to: none
version: 0.0.1

environment:
  sdk: ">=3.6.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  core:
    path: ../core
  equatable: ^2.0.8
  flutter:
    sdk: flutter
  geolocator: ^13.0.4
  injectable: ^3.0.0

dev_dependencies:
  build_runner: ^2.15.0
  flutter_test:
    sdk: flutter
  injectable_generator: ^3.0.2
